# This file lists the Python packages required for your Streamlit application.
# Streamlit Cloud reads this file to install dependencies.
# Add or remove packages as needed for your project.
# Pinning versions (==x.y.z) is recommended for reproducibility.

# Core Streamlit library
streamlit==1.33.0 # أو أحدث إصدار مستقر تحتاجه

# Data Handling and Analysis
pandas==2.2.2
numpy==1.26.4

# Visualization (اختر ما تحتاجه)
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.19.0

# Machine Learning (اختياري - أضف المكتبات التي تستخدمها مثل scikit-learn, tensorflow, pytorch)
# scikit-learn==1.4.1.post1

# Image Processing (اختياري - إذا كان تطبيقك يتعامل مع الصور)
Pillow==10.3.0
# opencv-python-headless # استخدم هذا إذا كنت تحتاج OpenCV بدون واجهة رسومية
# opencv-python-headless==4.9.0.80 # يمكنك تثبيت إصدار محدد إذا كان هناك مشكلة في الإصدار الأحدث (انظر الخطأ السابق)

# Utilities (أدوات مساعدة شائعة)
requests==2.31.0
python-dotenv==1.0.0 # إذا كنت تستخدم ملفات .env

# Add any other libraries your app uses here:
# your_package_name==x.y.z
